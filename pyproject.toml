[project]
name = "github-stats"
version = "0.1.0"
description = "GitHub statistics cards as SVG"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "httpx>=0.27.2",
    "redis>=5.2.0",
    "python-dotenv>=1.0.1",
]
